<!DOCTYPE html>
<html>
<head>
  <title>BGruppen</title>
  <%= stylesheet_link_tag    "styles" %>
  <%= javascript_include_tag "list.min" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id="content">
	<h1>Välkommen till AP och livet</h1>
	<h2>- ett snällt forum för nära föräldrar -</h2>
	<ul id="top_menu">
		<li>hem</li>
		<li>forum</li>
		<li>om oss</li>
		<li>om ap</li>
	</ul>
	
<div id="user_nav">
  <% if current_user %>
    Inloggad som: <%= current_user.name %>.
    <%= link_to "Logga ut", log_out_path %>
  <% else %>
    <%= link_to "Skapa ett konto", sign_up_path %> or
    <%= link_to "logga in", log_in_path %>
  <% end %>
</div>

<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}" %>
<% end %>

<%= yield %>
</div>
<div id="footer">
	<p>AP och Livet är ett privat forum för oss som uppfostrar våra barn enligt Attachment Parenting. Forumet är skrivet i ruby on rails av Hilla Elisabet Duka</p>
</div>
</body>
</html>
